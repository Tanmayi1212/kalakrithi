/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [],
    unoptimized: false,
  },
  // Enable static exports if needed
  // output: 'export',
};

module.exports = nextConfig;
